{
  "name": "Tournament",
  "type": "object",
  "properties": {
    "organization_id": {
      "type": "string"
    },
    "season_id": {
      "type": "string",
      "description": "Active season this tournament belongs to"
    },
    "name": {
      "type": "string"
    },
    "sport": {
      "type": "string",
      "enum": [
        "basketball",
        "volleyball"
      ]
    },
    "division": {
      "type": "string"
    },
    "start_date": {
      "type": "string",
      "format": "date-time"
    },
    "end_date": {
      "type": "string",
      "format": "date-time"
    },
    "num_teams": {
      "type": "number",
      "enum": [
        4,
        8,
        16
      ]
    },
    "status": {
      "type": "string",
      "enum": [
        "setup",
        "seeding",
        "in_progress",
        "completed"
      ],
      "default": "setup"
    },
    "current_round": {
      "type": "string"
    },
    "best_of_settings": {
      "type": "object",
      "default": {}
    },
    "initial_teams": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "is_manual_bracket": {
      "type": "boolean",
      "default": false
    },
    "manual_matches": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "default": []
    },
    "manual_connectors": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "default": []
    },
    "manual_sections": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "default": []
    }
  },
  "required": [
    "organization_id",
    "name",
    "sport",
    "num_teams"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "admin",
            "is_super_admin": true
          }
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "admin"
              }
            },
            {
              "data.organization_id": "{{user.data.organization_id}}"
            }
          ]
        }
      ]
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin",
            "is_super_admin": true
          }
        },
        {
          "data.organization_id": "{{user.data.organization_id}}"
        },
        {
          "data.organization_id": "{{user.data.active_organization_id}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "user_condition": {
            "role": "admin",
            "is_super_admin": true
          }
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "admin"
              }
            },
            {
              "data.organization_id": "{{user.data.organization_id}}"
            }
          ]
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "user_condition": {
            "role": "admin",
            "is_super_admin": true
          }
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "admin"
              }
            },
            {
              "data.organization_id": "{{user.data.organization_id}}"
            }
          ]
        }
      ]
    }
  }
}